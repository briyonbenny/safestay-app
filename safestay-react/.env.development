# Dev: npm run dev loads this file.
VITE_USE_API=true

# Same port as the Node app (check the listen line in the API terminal). Host must match the tab (localhost vs 127.0.0.1).
VITE_API_BASE_URL=http://localhost:5000
