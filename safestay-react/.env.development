# Auto-loaded by Vite when you run `npm run dev` from safestay-react (sibling API: ../web technolagy on port 3000).
# Leave VITE_API_BASE_URL unset so requests use /api and the Vite proxy forwards to localhost:3000.
VITE_USE_API=true
